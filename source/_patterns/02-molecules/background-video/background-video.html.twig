{% set classes = ['bg-video', 'background-image'] %}

{% for item in items %}

<div{{ attributes }}>
  {% include "@atoms/text/label/label.twig" %}
  <video{{ item.attributes.addClass(classes) }} alt="{{ item.alt }}" autoplay=""  id="{{ item.id }}" loop="" muted="" preload="auto" {% if {{ item.style }} %} style="{{ item.style }}" {% endif %}><source src="{{ item.source }}" /></video>
</div>

{{ button.content }}

{% endfor %}
